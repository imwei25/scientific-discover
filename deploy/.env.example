# 复制成 deploy/.env 并填入真实值
DEEPSEEK_API_KEY=sk-你的deepseek密钥
# 模型（provider/model 形式）。用你的 DeepSeek 账号里可用的模型名。
OC_MODEL=deepseek/deepseek-v4-pro

# ---- 多用户部署用 ----
# 基础域名（单域名路径路由）。用户访问地址 = https://<BASE_DOMAIN>/<用户名>/。
# 证书由 Caddy 自动签发/续期；user-add 只用它打印访问地址。
# 【别填免费动态 DNS 域名】duckdns/no-ip/3322 这一类在运营商与单位网络里按域名整片封禁，
# 表现是"IP 通、443 也通，但 TLS 握手被 RST / 明文 HTTP 直接 RST"。原委与实测见
# deploy-auth/Caddyfile.example 顶部注释；诊断脚本 scripts/net-diagnose/probe-sni.ps1 可当场归因。
BASE_DOMAIN=niuma.tellgen.com

# ================= 文献检索源 API key（都可选；见 deploy/检索通道-API配置.md） =================
# 服务器多用户共用一个出口 IP，匿名请求容易被各源限速（429）。填了下面的 key/邮箱后，
# 每源走独立更高的额度/礼貌池，稳定性大幅提升。留空 = 免费匿名档，功能不变、只是更容易被限速。
# 这些值经 render-compose.sh 自动注入每个用户容器，换服务器只需搬这一个 .env 文件。
#
# 【必填一项，免费】联系邮箱：进 Crossref/OpenAlex/Europe PMC 礼貌池、Unpaywall/NCBI 也要求它。
#   填一个真实邮箱即可（别用 example.com，Unpaywall 会拒）。一个变量覆盖所有源。
SCI_CONTACT_EMAIL=you@your-org.com
#
# 【强烈建议，免费】NCBI E-utilities key：PubMed 限速 3→10 req/s（最易 429 的源）。
#   领取：https://www.ncbi.nlm.nih.gov/account/ → Settings → API Key Management
#   量更大可免费申请增强 key（>10 rps）：邮件 eutilities@ncbi.nlm.nih.gov
NCBI_API_KEY=
#
# 【建议，免费】Semantic Scholar key：匿名池全体共享 100 次/5 分钟，领 key 后独享更高额度。
#   申请：https://www.semanticscholar.org/product/api（表单，免费）
S2_API_KEY=
#
# 【可选，用量计费】OpenAlex key：2026-02 起需 key，每天 $1 免费额度，纯元数据检索通常够用。
#   注册：https://openalex.org/  超额才计费；不填则走每日免费额度。
OPENALEX_API_KEY=
#
# 【可选，付费】Crossref Metadata Plus token：独立服务器池 + SLA，最小档 $550/年。
#   仅当公共池仍不稳时才需要；不填走免费礼貌池。https://www.crossref.org/services/metadata-retrieval/metadata-plus/
CROSSREF_PLUS_TOKEN=

# ================= OCR 图片识字 API key（可选；ocr 技能用） =================
# 【可选，免费】OCR.space key：把官方以图片发布的文件（如国自然申请代码表）、扫描件识别成文字。
#   邮箱注册即可、无需信用卡：https://ocr.space/ocrapi 。免费额度 Engine3 2500 次/月、单图 ≤1MB。
#   全体用户容器共用这一个 key 的配额；泄露/耗尽可在后台随时重置。留空 = ocr 技能报"未配置"。
OCR_SPACE_API_KEY=

# ============ 生图 API key（可选；mechanism-figure 机制示意图技能用） ============
# 阿里云百炼 DashScope 的 key，用来出机制图 / 通路示意图 / 图形摘要（qwen-image 系模型）。
#   开通：https://bailian.console.aliyun.com/ → API-KEY 管理。按张计费，有免费额度。
# ⚠ 风险与 OCR/检索 key 同一档，但要知道具体是什么：这个 key 会【注入用户容器】，
#   容器里的 agent 一句 env 就能读走 —— 也就是说任何用户都可能拿它去烧配额。
#   （主上游 LLM key 刻意不注入容器、走 manager 的 /llm 转发，正是为了避免这一点；
#   生图目前没有对应的转发通道，所以只能按"共用一把可随时重置的 key"处理。）
#   因此：① 在阿里云控制台给这把 key 单独设消费上限 / 用一个独立子账号；
#         ② 发现异常用量就在控制台直接重置，换完这里改一行、重跑 render-compose 即可；
#         ③ 不想承担这个风险就留空 —— 技能会明确报"未配置"，其余功能不受影响。
# 留空 = mechanism-figure 只能用 --dry-run（做提示词、不出图）。
QWEN_API_KEY=
# 可选：换模型（默认 qwen-image-2.0）。留空即用默认。
QWEN_MODEL=
