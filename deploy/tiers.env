# 用户档位（分级）定义。一行一个档位，空白分隔四列（数值列 0=不限）：
#   <档位名>   <每日成本上限USD>   <存储上限MB>   <模型名>
# <模型名>：该档位走 LLM 网关(one-api)时请求的模型名——需在 one-api 里有对应渠道；缺省 deepseek-v4-pro。
#   等以后在 one-api 里加了更便宜/更强的渠道，把某档的模型名一改，该档所有用户即路由到不同模型。
# 用法：users/<名>.env 里 TIER=<档位名>。改完 scripts/render-compose.sh 并重建容器生效。
# 额度按 USD/天（OpenCode session.cost，含缓存折扣）。括号 token 估算仅供直观：$0.30≈数十万、$1.50≈数百万/天。
#
# 档位名   每日USD   存储MB   模型名
free       0.30      1024     deepseek-v4-pro
plus       1.50      4096     deepseek-v4-pro
admin      0         0        deepseek-v4-pro
