# 用户档位（分级）定义。一行一个档位，空白分隔三列：
#   <档位名>   <每日成本上限USD>   <存储上限MB>
# 数值 0 = 不限。# 开头为注释。改完运行 scripts/render-compose.sh 并重启受影响容器即生效。
#
# 用法：在某用户的 users/<名>.env 里写 TIER=<档位名>，该用户即套用此表的额度。
#   若该 .env 里另有非空的 DAILY_COST_LIMIT= / STORAGE_LIMIT_MB=，则以那两行为准（个别用户单独加码/收紧的后门）。
#
# 额度按 USD/天计（用 OpenCode 的 session.cost，已含 DeepSeek 缓存折扣，比按 token 数更贴近真实成本）。
# 括号内 token 估算仅供直观参考（按 v4-pro 混合口径粗算，实际随输入/输出/缓存比例浮动）：
#   $0.30/天 ≈ 数十万 token/天；$1.50/天 ≈ 数百万 token/天。
#
# 档位名        每日USD   存储MB
free            0.30      1024      # 普通：轻度使用，约几十万 token/天、1GB 存储
plus            1.50      4096      # 高级：重度写作，约数百万 token/天、4GB 存储
admin           0         0         # 管理员/内部：不限额、不限存储
