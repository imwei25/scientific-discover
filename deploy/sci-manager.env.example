# sci-manager 的【可变配置】。setup.sh 会把它装到 /etc/sci-manager.env（chmod 600），
# 并且【只在文件不存在时创建、之后永不覆盖】——所以在这里改的值不会被重跑 setup.sh 冲掉。
# （历史教训：这些值以前直接写在 systemd 单元里，setup.sh 每次重装都用仓库模板覆写单元，
#   把运维调过的 WARM_CAP 和管理密码静默改回默认值，且 `enable --now` 对已运行服务不重启，
#   要等下次重启才发作——典型的延时炸弹。）
#
# 改完生效： sudo systemctl restart sci-manager

# ---- 网页管理台 /admin ----
# 留空 = 关闭管理台（默认，也是最安全的）。要开就设一个强密码。
# ⚠ 管理台能加删用户、改档位、往 LLM 网关塞渠道；设弱口令等于把服务器交出去。
# 生成强密码： openssl rand -base64 24
ADMIN_PASSWORD=

# ---- 容量 ----
# WARM_CAP：最多几个用户容器同时在跑（起第 N+1 个前先按 LRU 停掉一个真正空闲的）。内存是硬顶，绝不超配。
# 5 是在 2 vCPU / 3.4 GB 的机器上实测定的：5 人同时跑 md→PDF 全部成功，内存峰值 2668/3495 MB，
# swap 零使用、零 OOM。换机器请重新实测再改：容器空闲约 261 MB/个，重活时远不止。
WARM_CAP=5

# 容器空闲多久后回收（毫秒；默认 10 分钟）
IDLE_MS=600000

# 冷启动等待与抢容量等待（毫秒）
START_TIMEOUT_MS=60000
CAP_WAIT_MS=120000

# ---- 宿主侧额度账本 ----
# manager 另开一个记账端点给各用户容器上报/查询今日成本，账本落宿主 deploy/data/quota/，
# 容器里的 agent 摸不到 → 每日额度无法靠改 quota.json 绕过。
# ⚠ 云安全组/防火墙【不要】放行这个端口：它只该被本机容器（172.x 私网）打到；
#   端点自身也校验私网来源 + 每用户令牌（users/<name>.env 的 QUOTA_TOKEN，render-compose 自动补发）。
# 置空（QUOTA_LISTEN=）= 关闭，容器回落本地记账（额度可被容器内 agent 篡改，不建议）。
#QUOTA_LISTEN=0.0.0.0:8091
# 账本目录（默认 deploy/data/quota，已 gitignore）
#QUOTA_DIR=

# ---- LLM 转发通道（记账端点的 /llm/* 路径）----
# 上游共享 key 不再进容器：容器 opencode 拿每用户 QUOTA_TOKEN 打 manager 的 /llm，
# manager 换成真实 key 转发。key 优先取这里的 LLM_UPSTREAM_KEY，不填则自动回落读
# deploy/.env 的 DEEPSEEK_API_KEY（一般不用重复配）。
# 想让转发走 one-api 网关调度：LLM_UPSTREAM_URL=http://127.0.0.1:3010/v1 + LLM_UPSTREAM_KEY=<one-api令牌>
#LLM_UPSTREAM_URL=https://api.deepseek.com
#LLM_UPSTREAM_KEY=

# ---- one-api 管理接口（同供应商模型切换 + /admin 网关渠道管理）----
# 与上面的 LLM_UPSTREAM_* 是【两回事】：那是"转发用哪把上游 key"，这里是"manager 去 one-api
# 的【管理 API】读渠道/模型列表"。两者都留空时，前端的"切换同供应商模型"与 /admin 的网关面板
# 显示"网关未接入"。设了才启用（GATEWAY_ENABLED = 两者皆非空）。
# ONEAPI_TOKEN 是 one-api 的【系统访问令牌】(New-Api-User:1)，不是普通 sk- 渠道 key。
# ⚠ 历史坑：这两项以前被 inline 写死在 systemd 单元里，而 setup.sh 升级旧单元时曾漏迁它们
#   → 升级后网关功能静默失效。现已归位到本文件、setup.sh 也会从旧单元迁移它们。
#ONEAPI_URL=http://127.0.0.1:3010
#ONEAPI_TOKEN=
