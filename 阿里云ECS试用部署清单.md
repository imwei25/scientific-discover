# 阿里云 ECS 试用部署清单（前半程：买机 → 登录 → 安全组）

> 适用：**ECS 300 元免费额度 / 2vCPU 4GiB / 香港地域 / Ubuntu 22.04**。
> 本文只讲你不熟的"服务器到手之前"这段；服务器登录之后的环境安装与跑通，接着看
> [opencode服务器部署方案.md](opencode服务器部署方案.md) 的「第 2 节 + 阶段 A/B」。

---

## 0. 先说清楚这台试用机能做到哪

- **4GiB 内存**够做手册里的 **阶段 A（不用 Docker 先跑通）** 和 **阶段 B（打成一个 Docker 镜像、单容器跑）**——足以证明"这个项目在服务器上完整能跑"，含 PDF 排版。
- **不够做阶段 C（多用户，每人一个 2g 容器）**——那要等正式上线换 4核8G 及以上的付费机。试用阶段目标就是**学会 + 跑通单实例**。
- 选**香港地域**的关键原因：大陆地域对外开 80/443 + 绑域名要 **ICP 备案**（几天~几周），香港**免备案**，试用最省事。

---

## 1. 购买 / 开通 ECS（在阿里云控制台点选）

登录阿里云 → 进 **ECS 云服务器** → 走免费试用入口或"创建实例"。逐项按下面选（控制台措辞偶有变动，认准这些**关键选择**即可）：

| 选项 | 选什么 | 说明 |
|---|---|---|
| 付费方式 | 免费试用 / 抵扣 300 元额度 | 试用结束或额度用完会转计费，见第 6 节 |
| 地域 | **香港** | 免备案；离大陆近、延迟低 |
| 实例规格 | **2 vCPU / 4 GiB** | 内存是瓶颈，别选 1GiB |
| 镜像 | **Ubuntu 22.04 64位** | 手册以此为例，最省心 |
| 系统盘 | **ESSD ≥ 40 GB** | Docker + texlive 镜像占几个 GB，别只给 20GB |
| 公网 IP | **分配公网 IPv4**，带宽按量或 1–3 Mbps 固定 | 没公网 IP 就连不上 |
| 登录凭证 | 设 **root 密码**（或密钥对） | 新手用密码更直观；想更安全用密钥对 |
| 安全组 | 先勾 **放行 22(SSH)** | 其余端口见第 3 节，试用阶段能不开就不开 |

创建后在实例列表记下**公网 IP**（形如 `47.x.x.x`）。

---

## 2. 首次登录服务器（Windows）

你本机是 Windows 11，两种方式都行：

- **最省事**：控制台实例页 → **远程连接 / Workbench** → 网页里直接开终端（不用装任何东西）。
- **常规**：打开 PowerShell 或本项目的 Bash，敲：
  ```bash
  ssh root@你的公网IP
  ```
  首次问 `yes/no` 输 `yes`，再输第 1 节设的 root 密码（输密码时屏幕不显示，正常）。

登录成功会看到 `root@iZ...:~#` 提示符。

---

## 3. 安全组端口清单（哪些该开、哪些绝不能开）

在 ECS 控制台 → 实例 → **安全组 → 配置规则 → 入方向**里加：

| 端口 | 试用阶段 | 正式上线 | 用途 |
|---|---|---|---|
| **22** | ✅ 必开 | ✅（建议限来源 IP） | SSH 登录 |
| **8080** | ⛔ 先不开 | ✅ | 阶段 C 的 nginx 登录入口 |
| **80 / 443** | ⛔ 先不开 | ✅ | 正式域名 + HTTPS（阶段 D 的 Caddy） |
| **3000 / 4098** | ⛔ **永不对公网开** | ⛔ **永不对公网开** | 网关/OpenCode 内部端口，只走本机 |

> 试用阶段验证网页，**不用开任何 web 端口**——用 SSH 隧道把服务器的 3000 映射到你本机（见第 5 节），更安全。

---

## 4. 装环境 + 跑通（转到已有手册）

SSH 登进去后，**照 [opencode服务器部署方案.md](opencode服务器部署方案.md) 做**，但先做下面这个 4GiB 专属的一次性准备：

```bash
# 4GiB 内存偏紧：建一个 4GB swap，避免 docker build texlive 镜像 / 跑 pandas+xelatex 时 OOM
sudo fallocate -l 4G /swapfile && sudo chmod 600 /swapfile
sudo mkswap /swapfile && sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
free -h    # 应看到 Swap: 4.0Gi
```

然后按手册顺序：
1. **手册第 2 节**：装 git / Node 20 / Python / Docker，再把代码弄上服务器
   （有远程仓库就 `git clone`；没有就用 `scp -r` 把整个项目目录传上去）。
2. **手册 阶段 A**：不用 Docker 先跑通，浏览器验证一次（证明服务器能跑）。
3. **手册 阶段 B**：`docker build` 打镜像、单容器跑——这一步会把 **pandoc + texlive + CJK 字体**一起装进镜像，`render-pdf-doc` 出中文 PDF 即可用。
4. 试用阶段**到此为止**，先不做阶段 C（多用户）。

DeepSeek key 按手册用 `export DEEPSEEK_API_KEY=sk-...`（阶段 A）或写进 `deploy/.env`（阶段 B）；模型名 `deepseek-v4-pro`，别用 `deepseek-chat`。

---

## 5. 浏览器验证（SSH 隧道，不开公网端口）

在**你自己电脑**（不是服务器）的 PowerShell/Bash 里：

```bash
ssh -L 3000:127.0.0.1:3000 root@你的公网IP
```

保持这个窗口不关，本机浏览器打开 `http://localhost:3000`，试：
- 发"你好，你能做什么" → 看到 DeepSeek 流式回复；
- 上传一个 CSV → 说"分析 uploads/xxx.csv 做线性回归并画图" → 生成 `outputs/*.png`、右侧出下载链接。

到这一步就说明**整套在服务器上跑通了**。

---

## 6. 试用计费 · 避免莫名扣费（重要）

- 300 元额度 / 试用期是有期限的，**到期后会转按量或包月计费**。
- 不用时：控制台把实例**停机**（按量付费停机不计算力费，但**云盘仍按量计费**）；彻底不用就**释放实例**（会删数据，先做快照或备份）。
- 想保留成果再释放：先给系统盘打**快照**，或按手册阶段 D-④ 备份 volume。
- 建议在阿里云设**费用预警**，额度快用完时短信提醒。

---

## 速查：这次试用你要做的最小闭环

1. 控制台买机：香港 / Ubuntu 22.04 / 2vCPU 4GiB / 40GB / 开 22 端口 / 记公网 IP
2. `ssh root@IP` 登录 → 建 4G swap
3. 手册第 2 节装环境 + 上代码 → 阶段 A 跑通 → 阶段 B 打 Docker 镜像
4. 本机 `ssh -L 3000:...` 隧道 → 浏览器 `localhost:3000` 验证对话 + 出图
5. 不用就停机/释放，注意额度期限
