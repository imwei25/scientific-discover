# 密钥与虚拟环境（保留在磁盘上，但不进 git）
backend/.env
backend/.venv/

# 旧产品的遗留目录（未跟踪的构建产物，不进 git）
backend/
frontend/
src-tauri/

# 运行时产物
outputs/
uploads/
node_modules/
*.log

# 部署密钥与密码文件（绝不入库）
deploy/.env
deploy/htpasswd
.env
serve.out
serve.err
