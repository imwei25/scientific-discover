# 发布说明 v0.1.39 (2026-08-19)

## 更新内容

1. 聊天接入：微信 / 企微里的每条回复末尾，现在真的会带上「来自 Niuma Science 科研小助手」的落款了——此前这行落款一直没发出去（两个平台都没有，只有定时任务的推送带着），是发送时机比聊天软件定稿慢了一步。
2. 聊天接入：去掉回复末尾那行「模型名 · 工作目录」的技术信息，聊天窗口只留正文和落款。
3. 企微接入教程：写明创建智能机器人时**必须选「API 模式」**——选成零代码 / 工作流那类模式的机器人没有可用的 Bot ID + Secret，填进来连不上，只能重建一个。
4. 侧栏：点微信 / 企微图标跳到对应会话时，左侧会话列表会跟着把那一行露出来；开机恢复上次会话时同样会滚动到它。
