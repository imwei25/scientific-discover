# 发布说明 v0.1.41 (2026-08-20)

## 更新内容

1. 聊天接入：定时任务推送到手机后，回复 `/task` 即可切到该任务的会话直接追问（它才知道任务做了什么、产物在哪）；`/back` 切回你原来所在的会话（不在任务会话上时，回到绑定的会话）。推送尾端会附上可用指令说明（含 `/new` 开新会话、`/stop` 中断任务）。每次重新绑定会话后默认回到绑定会话路线。
2. 文献导读：额外产出一份 Word 版，便于直接批注修改。
3. 界面：刷新按钮现在会同时刷新当前会话与「产出」侧栏。
