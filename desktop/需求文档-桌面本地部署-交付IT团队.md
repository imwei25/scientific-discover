# 需求文档：科研医学 Agent 桌面本地部署版（交付 IT 团队）

> 版本：v1.0（2026-07-28）｜需求方：产品/技能负责人｜实施方：公司 IT 团队
>
> 本文档只描述**需求与验收标准**，不限定技术路线。文末附一份**已验证的 PoC 资产清单**
> （分支 `feat/desktop-windows`，含可运行的安装器与全部验证结论），IT 团队可直接复用、
> 部分复用或另行选型——但凡另行选型，需覆盖同样的验收项。

---

## 0. 一句话目标

把现有"云端多用户 SaaS"形态的科研医学 Agent，做成**客户 Windows 电脑上一键安装的桌面应用**：
算力与数据全部在客户本机，云端收缩为「用户身份/鉴权 + token 计量管控」的瘦服务。
商业控制点 = 云端 key：不续费/停用 → 产品失能。

## 1. 背景与现状（供理解，非需求）

- 现有产品：多用户服务器部署（Docker 容器/用户 + manager 生命周期管理 + one-api LLM 网关），
  代码仓库 `sci-agent`，生产分支 `feat/multiuser-deploy`。
- 应用本体三层：web 网关（node, `web/server.mjs`）→ opencode agent 引擎 → 23 个技能
  （SKILL.md 提示词 + Python/Bash 脚本，Python 栈钉版于 `deploy/requirements.txt`）。
- **两种形态必须共用同一份应用代码与技能目录**（这是硬需求，见 §3.6）。

## 2. 角色与职责边界

| 角色 | 负责 | 不负责 |
|---|---|---|
| **技能负责人（需求方本人）** | `.opencode/skills/` 全部技能内容、`AGENTS.md` 主控指令、`deploy/requirements.txt` Python 依赖钉版 | 打包、分发、云端服务、运维 |
| **IT 团队** | 桌面客户端（打包/安装/启动/更新）、云端服务（注册登录/key 管理/计量/后台）、发布运维 | 技能内容本身 |

**接口约定（最重要的一条）**：技能目录 + AGENTS.md + requirements.txt 就是双方的全部接口。
技能负责人更新这三样 → IT 的构建管道自动吸收进下一版客户端；IT 不修改技能内容，
技能负责人不关心打包细节。技能有跨平台约束时以本文 §3.6 为准。

## 3. 需求清单

优先级标注：【必须】v1 上线前提；【应当】v1 尽量、v1.x 必须；【可选】有条件再做。

### 3.1 桌面客户端

- 【必须】一键安装：单文件安装器 exe，Win10/11 x64，**无需管理员权限**、无需客户预装任何
  环境（含 Python/Node/Git；WebView 运行时缺失时自动补装）。
- 【必须】自包含运行：安装后即含完整运行时（agent 引擎、Python 科研栈、文档转换工具、
  技能全集）。除 LLM 调用与文献检索外不依赖外部环境。
- 【必须】启动体验：双击图标 → 加载页 → 主界面（现有 web 界面）。后台引擎进程**全程无窗口**、
  不在任务栏出现；应用退出时后台进程**全部回收**，不留残留进程/占用端口。
- 【必须】进程与端口安全：agent 引擎端口只绑 127.0.0.1（引擎无鉴权，绝不可暴露到局域网）；
  web 网关如对局域网开放，必须有本地密码门禁（现有机制可复用）。
- 【必须】故障可诊断：后台日志落盘；提供一个客户现场可执行的自检脚本/入口，能一次性验证
  运行时完整性（Python 栈可用、文档转换可用、技能调用契约成立）。
- 【应当】客户端自动更新：新版本发布后客户端能提示/静默升级（含技能更新，见 §3.5）。
- 【应当】首次启动引导：未配置云端账号/key 时给出引导页，而不是裸报错。
- 【可选】纯单机模式开关：网关也只绑 127.0.0.1，彻底不对局域网开放。

### 3.2 云端：用户身份与登录

- 【必须】注册/登录方式：**手机号 + 短信验证码**（一期）；**微信扫码**（二期，需公司主体的
  微信开放平台"网站应用"资质，请 IT 评估办理周期并提前启动）。
- 【必须】云端维护用户档案：身份（手机号/openid）、**档位（分级）**、状态（正常/停用）、用量。
  分级管理是硬需求：不同档位对应不同模型与额度。
- 【必须】登录成功后云端向客户端下发一个**有效期内有效的 key**（含到期时间、可用模型、档位信息）；
  客户端缓存后，**有效期内再次启动免登录**；临近到期静默续期；过期/被停用 → 引导重新登录。
- 【必须】key 即管控点：云端可随时对单个用户**停用/调额/调档**，效果在下一次 LLM 调用即生效
  （客户端收到 401/额度耗尽时给出明确提示而非裸报错）。
- 【应当】一人多机策略：默认同一账号多台机器共用额度；预留按设备踢下线的扩展空间。

### 3.3 云端：token 计量与计费

- 【必须】按用户记量：每个用户的 token 消耗可查（日/月/累计），作为计费与额度依据。
- 【必须】额度控制：按档位设额度上限，超限拒绝并提示；额度策略可由运营调整，无需发版。
- 【必须】上游模型通道管理：多渠道调度/故障切换/换模型对客户端透明（现有 one-api 已具备，
  建议评估直接复用：其 token 原生支持有效期+额度，与 §3.2 的"有效期 key"天然吻合）。
- 【应当】用量异常告警：单账号用量突刺提醒运营（防 key 外借/泄露滥用）。

### 3.4 云端：运营后台

- 【必须】最小后台能力：用户列表（搜索）、改档位、停用/启用、发/重置 key、看用量。
  形态不限（网页后台或成熟工具皆可），但操作不得要求登录服务器敲命令。
- 【可选】数据看板：活跃用户、总用量趋势。

### 3.5 版本与技能分发

- 【必须】构建管道：从代码仓库一键构建出安装器（技能目录、AGENTS.md、requirements.txt
  从仓库直接吸收，**不得在打包环节手工拷贝/修改技能**）。
- 【必须】版本可追溯：每个安装包有版本号；云端能看到各用户客户端版本（随 LLM 调用或心跳上报）。
- 【应当】技能热更新：技能内容更新不必重发整个安装包（技能目录体积 <100MB，可增量分发）；
  由此技能负责人的更新节奏与客户端发版解耦。
- 【应当】灰度：新版本可先发部分用户。

### 3.6 跨平台契约（技能侧已承诺、打包侧必须保障）

以下是技能正常运行的环境契约，**无论 IT 采用什么打包方案都必须成立**（PoC 已验证一种成立方式）：

1. 项目根存在 `.venv`，且 `.venv/bin/python`（POSIX 风格路径）与 `.venv\Scripts\python.exe`
   **都能执行**并加载同一套科研 Python 栈（钉版 `deploy/requirements.txt`）；
2. 环境变量 `REPO_ROOT` 指向应用根（正斜杠路径），bash 中 `${REPO_ROOT:-/app}` 展开正确；
3. 命令行环境提供：`bash`（支持 heredoc/管道/source）、`git`、`pandoc`、裸 `python3`；
4. matplotlib 中文不出豆腐块（系统级 rc 兜底，含逐字形回退）；
5. 每个会话有独立工作目录（产物目录），agent 写裸文件名即落对应会话目录（现有网关机制）。

技能负责人承诺：技能只依赖以上契约，不再引入新的系统级依赖；如需新增（例如新工具二进制），
提前与 IT 协商进包。

### 3.7 安全与合规

- 【必须】agent 引擎不对外暴露（见 §3.1）；key 传输走 HTTPS。
- 【必须】隐私口径：对外宣传"数据不出本地"时，必须同时声明"提交给 AI 分析的内容
  （含脱敏后文本）会发送至云端大模型"。产品内置 deidentify 脱敏技能，但说明文档需把
  "含患者数据先脱敏"写为客户义务。
- 【必须】云端不存储用户内容数据（会话/文件都在客户本机），只存身份/档位/用量。
- 【应当】客户端凭据保护：key 本地存储位置权限收敛（接受"无法对本机管理员保密"的现实，
  防滥用主要靠有效期+额度+可踢，见 §3.2/3.3）。

### 3.8 明确的非目标（v1 不做）

- 不做离线运行（LLM 与文献检索必须联网）。
- 不做技能内容的强 IP 保护（技能明文随包分发；护城河=云端 key + 持续更新）。
- 不做本地多账号（一机一套数据；客户内部门禁用现有局域网密码机制）。
- 不做 macOS/Linux 客户端。
- 不打包 texlive（出版级 PDF 排版）与 LibreOffice（pptx 在线预览）：体积原因刻意降级，
  客户机自装后功能自动恢复；安装器可【可选】提供"完整版"变体，由 IT 权衡。

## 4. 验收标准（全部满足即 v1 通过）

| # | 场景 | 通过标准 |
|---|---|---|
| A1 | 全新 Win11 家用机（无任何开发环境）双击安装 | 无管理员权限完成安装；桌面/开始菜单有图标 |
| A2 | 首次启动 | ≤60 秒进入主界面；无任何黑窗口闪现 |
| A3 | 注册登录 | 手机号+验证码完成注册；客户端获得 key 进入可用状态 |
| A4 | 免登录 | 重启应用不再要求登录（key 有效期内）；到期前静默续期 |
| A5 | 对话+技能 | 上传 csv → 统计分析出中文图（无豆腐块）；生成 docx 排版文件；产物在界面侧栏可下载 |
| A6 | 计量 | 云端后台能看到该用户本轮的 token 消耗 |
| A7 | 停用 | 后台停用该用户 → 客户端下一轮对话被拒并提示重新登录/联系客服；重新启用即恢复 |
| A8 | 额度 | 超出档位额度被拒并有明确提示 |
| A9 | 调档 | 后台调档 → 用户可用模型/额度随之变化 |
| A10 | 退出 | 关闭窗口后无残留后台进程、端口全部释放 |
| A11 | 安全 | 局域网内其他设备无法访问 agent 引擎端口；访问网关必须输本地密码 |
| A12 | 卸载 | 控制面板正常卸载，无残留服务 |
| A13 | 诊断 | 客户现场跑自检脚本能输出各运行时组件的通过/失败结论 |
| A14 | 技能更新 | 技能负责人改一个 SKILL.md 合入仓库 → 走管道到达客户端（热更新或下一版安装包），无手工搬运 |

## 5. 已验证 PoC 资产清单（分支 `feat/desktop-windows`，可复用可替换）

| 资产 | 内容 | 状态 |
|---|---|---|
| `desktop/bundle.ps1` | 组装 1.7GB 自包含运行时（便携 node / opencode 独立二进制 / PortableGit / pandoc / 嵌入式 Python3.12+钉版科研栈），国内镜像优先 | ✅ 可复现 |
| `desktop/launcher/` | Tauri2 壳：无窗口拉起后台、健康轮询、开窗、退出清进程树；NSIS per-user 安装器 | ✅ 已出包 381MB，本机装测全绿 |
| `desktop/smoke.sh` | §3.6 契约自检脚本（对应验收 A13 的雏形） | ✅ 全绿 |
| `desktop/方案说明-本地部署与云端职责划分.md` | 架构对比、职责划分、登录/有效期 key 的一种设计（云端复用 one-api token 过期+额度） | 参考 |
| 验证结论 | 网关+引擎原生 Windows 全绿；真实 LLM 链路打通；技能命令契约（§3.6 全项）在包内环境全绿；引擎实测只绑 127.0.0.1 | 参考 |
| 已知坑 | PowerShell 脚本中文注释需 UTF-8 带 BOM；嵌入式 Python 装 sdist 需先装 setuptools；构建期间不可改资源目录 | 参考 |

## 6. 交接与协作方式

- 代码仓库、分支、PoC 全部移交 IT；技能负责人保留 `.opencode/skills/`、`AGENTS.md`、
  `deploy/requirements.txt` 的维护权（建议以 CODEOWNERS/评审规则固化）。
- 需求疑问找需求方澄清；技术选型（是否沿用 Tauri/one-api、云端语言框架、短信服务商等）
  IT 自主决策，以验收标准为准。
- 建议里程碑：M1 客户端可装可用（A1/A2/A5/A10–A13）→ M2 登录与 key 生命周期（A3/A4/A7）
  → M3 计量分级与后台（A6/A8/A9）→ M4 更新管道（A14）。
