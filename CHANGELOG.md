# 更新日志 CHANGELOG

本文件积累「自上次发布以来」的更新点；每逢大版本发布时，把「未发布」区的要点
并入该版本的 Release 说明（`scripts/release-notes.md`）一起对外发布，然后清空「未发布」区。

只记录用户能感知到的体验变化（现在能做什么 / 不再出什么问题 / 界面哪里变了），
不写内部实现细节（变量、接口、文件、协议等留给 commit message）。

## 未发布

### 写标书

- **修复：技术路线图/甘特图现在会边写边渲染成图**：写标书生成到 ```mermaid``` 图表时，
  以往要等整篇写完才显示成图（撰写过程中一直是代码文本）；现在图表一旦写完整就立即渲染成图，
  不必等全文结束。

### 找选题 / 写标书

- **新增：与 Zotero 打通**：找选题和写标书都能把文献与本机 Zotero 互通。若 Zotero 正开着
  （并在 设置→高级 勾选「允许本机其它应用与 Zotero 通信」），页面上会出现「🔗 从 Zotero
  导入」——选一个分类就能把里面的文献带进当前文献池；还能「🔗 推送到 Zotero」把检索或带入
  的文献存回 Zotero（可整池推，也可在找选题的文献列表里勾选几篇只推选中的）。Zotero 没开也
  不影响：继续用「📥 导入文献」拖入 Zotero 导出的 .ris/.bib 文件即可。文献导入 / 导出旁
  新增「?」帮助按钮，点开有直连与文件互导两种方式的图文步骤说明。

- **新增：找选题可"只用自己的文献"**：文献来源可选「自动检索 / 导入+再补检索 / 只用导入的
  文献（跳过检索）」。带着自己整理好的一批文献时，可跳过联网检索，直接据此生成研究现状综述
  与候选选题；也可先导入再补检索，把你的文献和新检索到的合并起来写。

- **新增：上传"文风样例"，让 AI 照你的文风写标书（兼去 AI 味）**：在写标书表单里可上传
  一份你满意的 Word/PDF/txt（如以往的标书或论文），点「提炼文风」后 AI 会总结出一份可编辑的
  「文风档案」（句式节奏、用词、语气、段落习惯等）；撰写与逐节重写时按此风格下笔，「去 AI 味」
  也会向它靠拢。只学“怎么写”，不会把样例里的内容或事实写进你的标书；不上传或关掉开关即维持原样。

- **新增：把鼠标移到文献引用上，浮现"是哪句原话支持了这个观点"**：调研报告和标书
  正文里，每处引用文献的地方，AI 会附上支持该论断的那一句英文原文摘录；把鼠标悬停
  （或键盘聚焦）到引用上，就会浮出一张小卡片，显示文献题名和这句支持句，不用再点开
  原文逐段找。生成后会自动核验每句支持句是否确为摘要里的原话，引用核验栏会告诉你
  「其中 N 处附有原文支持句」，若有个别没能在摘要里逐字对上也会提示你核对，杜绝编造。

- **新增：评审自查升级为「评审组模拟评审」**：初稿写完后不再是一段泛泛的自查，而是
  4 位虚拟评审专家（同行专家 / 方法学与统计专家 / 形式审查专家 / 挑剔评委）各自独立
  打分和挑问题，再合议出一份共识报告——包含每章节 0-10 分的评分表、A/B/C 资助建议、
  按严重度排序的问题清单（尽量附真实文献依据）与提交前完善清单。评审侧重会按资助
  类型自动调整（青年看潜力聚焦、面上看系统性深度、地区看依托条件）。
- **新增：申报要求覆盖度清单**：评审报告里逐项核对「科学问题是否凝练、创新点是否
  说清差异、技术路线有无备选方案」等 12 项申报要求，✅/⚠️/❌ 一目了然。
- **新增：按评审意见一键修订**：评审指出问题的章节，正下方直接显示问题清单，点
  「按评审意见修订」即自动重写该节；也可「⚡ 一键修订评审认定的薄弱章节」（低于 8 分的
  逐节自动改）。改完点「🔁 重新评审」重新打分，看看分数有没有提上来——形成
  「评审 → 修订 → 再评审」的闭环。
- **新增：技术路线图与甘特图**：研究方案一节会自动画出技术路线图，年度计划一节
  自动画出甘特图（Mermaid 图表，界面里直接渲染成图；导出 Markdown 时保留图表代码）。
- **新增：申报合规提醒**：评审报告末尾固定附上合规须知（生成式 AI 内容不得直接
  提交、引用需逐条核实、限项与占位符自查），对齐基金委对 AI 使用的规范要求。

### 全局

- **新增：不同环节可用不同 AI 模型**：在配置文件里加一行（如
  `LLM_STAGE_GRANT_REVIEW_MODEL=deepseek-reasoner`）就能让某个环节（比如标书评审）
  单独换更强的推理模型甚至另一家服务商，其余环节继续用默认便宜模型；用异构模型做
  标书「外审」可避免同一个模型自写自夸。每个环节当前用什么模型可访问
  `/api/config/stages` 查看，详见《使用说明》。

### 数据分析

- **新增：分析出结果后可「继续对话」反复改**：跑完一次分析后，下方多了一个输入框，
  可以直接提新需求（如"把柱状图改成箱线图并标注显著性""加一个按性别的亚组分析"
  "配色换成柳叶刀风格""对数变换后重跑"），AI 会在现有分析的基础上改一版并重新执行，
  不必从头再来。（续跑需要原始数据，若文件已移除请重新上传同一份数据。）
- **改进：图表导出的文字现在可编辑**：导出的 SVG / PDF 图，文字保留为真正的文本
  （而非转成曲线），可在 Illustrator / Inkscape 里选中、搜索、微调，满足期刊返修要求。
- **改进：长结论/复杂分析代码不再被中途截断**：放宽了 AI 单次输出的长度上限，
  内容更完整；输出仍是流式的，随时可点「停止」中断。
- **改进：图更接近期刊风格**：图例默认无边框、坐标轴更细、网格更淡；柱状图带黑色
  描边与误差棒；剂量/亚组等有序梯度用同一颜色深浅表示而非杂色；每张图配色更克制，
  红/绿只用来表示"升高/降低"等方向。为避免误导，柱状图的数值轴一律从 0 开始。
- **改进：图注更规范、可脱离正文阅读**：一键生成的图注改用 Nature 系惯例——加粗名词
  总题、a/b/c 分面简述、把样本量/误差类型/统计检验写进图注，且只写分析里确实出现过的
  数字，不编造。

### 桌面版

- **修 bug：右上角不再一直显示「（无项目）」**：桌面版的本地服务启动需要 20-30 秒，
  此前界面等不到它就永久进入"离线"状态——右上角只剩「（无项目）」、无法新建或切换项目。
  现在会自动等待服务就绪并恢复正常（期间界面照常可用，离线期间的输入也会在恢复后
  自动并入项目）。
