# 更新日志 CHANGELOG

本文件积累「自上次发布以来」的更新点；每逢大版本发布时，把「未发布」区的要点
并入该版本的 Release 说明（`scripts/release-notes.md`）一起对外发布，然后清空「未发布」区。

只记录用户能感知到的体验变化（现在能做什么 / 不再出什么问题 / 界面哪里变了），
不写内部实现细节（变量、接口、文件、协议等留给 commit message）。

## 未发布

- 「找选题」改为四步向导：填研究方向 → 确认检索设置 → 复核文献 → 生成调研报告，全程可返回上一步而不丢失后续内容；不再左右分屏。
- 文献复核这一步把检索结果与证据表合并：每篇文献下方直接用红色标出「核心发现」，默认全选，可增删/导入/导出后再生成。
- 检索设置支持按「最近 1–5 年 / 不限」筛选（默认近 3 年）；简化了界面，去掉了 PICO 提取与多余提示。
- 生成产出用「选题卡」呈现候选方向，不再让你把候选选题读两遍。
- 首页的「牛马」插画改得更圆润，可用鼠标拖动旋转；侧栏改用同款手绘小图标。
- 上传附件支持一次选择/拖入多个文件；上传框做得更紧凑。
- 「写标书」改为两步向导：准备材料 → 一键生成并在预览里精修，不再左右分屏、不再要你先确认大纲。
- 从「找选题」点"用此方向写标书"会直接按默认配置生成初稿并跳到预览，中途可暂停/继续。
- 准备材料只保留一个「附加材料」框（可粘贴或一次上传多个文件）+ 单独的「文风样例」上传框，去掉了零散输入框。
- AI 精修改为直接作用在正文上（选中一段或写意见即改、就地标红可撤回），编辑 / 去 AI 味 按钮保留；去掉了"逐节重写"，新增"一键全部重写"。
- 评审自查与正文分离：默认收起，需要时点开查看，可在修改后手动"重新评审"。
- 标书正文引用改为规范的数字 [1] 形式，文末自动附「参考文献」（GB/T 7714 数字著录）。
- 导出的 Word / PDF 字体排版更规整：中文正文宋体、标题黑体分级、正文首行缩进、行距加宽，不再像纯 Markdown 那样朴素。
