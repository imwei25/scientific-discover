# 更新日志 CHANGELOG

本文件积累「自上次发布以来」的更新点；每逢大版本发布时，把「未发布」区的要点
并入该版本的 Release 说明（`scripts/release-notes.md`）一起对外发布，然后清空「未发布」区。

## 未发布

### 入口改进循环（自动化）：数据分析与写作(analyze) 第 1 轮

- **打通 xlsx 的 KM/ROC 分析**：此前 xlsx 无法前端解析表头，列映射区提示「请直接输入列名」却没有输入框，
  生存分析/ROC 彻底走不通。现在无表头时列映射退化为可手动输入列名（提示需与首行列名完全一致），补上这条死路。
- **结论区一键复制**：新增「复制结论」按钮，直接把分析结论复制到剪贴板，不必再下载文件或手动划选。
- **结论工具栏编组**：导出/去排版几个按钮用 `.result-actions` 包裹右对齐，不再被 `space-between` 拉散得松松垮垮。

### 入口改进循环（自动化）：伦理材料(ethics) 第 1 轮

- **必填校验，别再下载空白送审件**：每种材料标出伦理委员会最看重的核心必填项（红色「必填」标记），
  未补齐时下载按钮禁用，并在下方列出「还差哪几项」——从源头减少送审被退回。
- **「从实验规划导入」不再静默失败**：导入成功提示「已导入 N 项」，没有可导入内容时明确提示
  「请先在『实验规划』里填写或生成方案」，不再点了没反应。

### 入口改进循环（自动化）：实验规划(plan) 第 1 轮

- **修 bug：样本量真正进方案**：此前样本量计算器点「使用此参数」只是存起来、显示「已保存到方案」，
  但生成实验计划时**并没有把这个 N 传给 AI**。现在确定的样本量（每组 N、场景、α、power、效应量）
  会作为事实带入「生成实验计划」与「SAP」，并提示临床按失访率上浮——不再出现「方案里的样本量和我算的对不上」。
- **禁用按钮会自解释**：研究想法为空时，按钮下方提示「请先填写你的研究想法 / 课题」。
- **按钮行不再溢出**：`.form-actions` 加 `flex-wrap`，一行 5 个生成按钮在窄窗/缩放下自动换行。

### 入口改进循环（自动化）：写标书(grant) 第 1 轮

- **禁用按钮会自解释**：题名与调研报告都为空时，生成按钮下方显示一行提示「请至少填写项目题名，
  或粘贴一份选题调研报告」，不再让人对着灰按钮以为程序坏了。
- **修「必填」自相矛盾**：题名此前标注「必填」，但实际只要题名或调研报告填其一即可开始——
  去掉误导的「必填」红标，规则与提示一致。
- **「收起」改「返回修改（内容不丢）」**：该按钮实际是返回上方表单（已填内容与已生成大纲都保留），
  原「收起」措辞会让人误以为内容被丢弃。

### 入口改进循环（自动化）：找选题(idea) 第 1 轮

- **调研报告一键复制**：结果工具栏新增「复制」按钮，把报告（含参考文献）复制到剪贴板，
  成功短暂显示「已复制 ✓」——不必再为拿一段文字去下载整个 Word。
- **结果工具栏防溢出**：`.result-actions` 加 `flex-wrap`，按钮多时自动换行右对齐，
  不再在窄画布里横向溢出/被截断。
- **状态文字排版修正**：去掉对中文无效的 `text-transform:uppercase`、收紧字距，中文状态不再发虚。

### 入口改进循环（自动化）：首页(home) 第 1 轮

- 启动「入口改进循环」：每轮对一个入口派 agent 批判 → 制定小改进 → 执行 → 记录到
  `docs/entry-improvements/`。首轮针对**首页**。
- **首页概览文案**：「完整工作流 · 从想法到投稿」→「覆盖选题到投稿 · 每个工具都能单独用」，
  消解「9 个工具必须按 01–09 顺序走完」的误解（它们相互独立、可单独使用）。
- **清理首页死/冲突 CSS**：删除已不再渲染的 `.home-art-wrap`/`.hero-svg`（旧分子骨架样式），
  以及指向不存在类的 `.home .home-grid { animation:none }` 和与 `.home-text …` 重复的冗余动画规则；
  保留概览小标题淡入。纯卫生清理，无视觉回归。

### 首页装饰：换成手绘 3D「科研牛马」+ 修排版对齐

- 首页 hero 的两个自转 emoji 立方体（🐂/🐎）换成**一只手绘的低多边形 3D 牛马**：分面着色
  （顶面亮 / 正面中 / 侧面暗 / 底最暗）撑出体积感，牛头双角 + 马鬃马尾。纯 inline SVG，配色
  全走主题 token（含 midnight 深色），仅身体轻微漂浮、影子留在地上，保留 `prefers-reduced-motion` 关停。
- **点题「科研」**：牛戴学究圆框眼镜，背上一叠稿纸上立着锥形瓶——一眼看出是「科研牛马」。
- 去掉图下方的小字「牛马」。
- **修首页排版对齐**：「完整工作流」区原来 `max-width:1080 + margin:auto` 居中，比标题
  `niuma-research` 内缩了一截；改为与 hero 同宽齐左，标题、工作流小标题、卡片网格左边线对齐。

### 数据分析：提升任务成功率（T2 探索轮 / T3 方法路由 / T4 修复循环与红线）

- **探索轮（T2）**：正式写分析代码前，先让 AI 写一段“只读探索代码”在真实数据上跑一遍，把
  真实观测（列是否存在、分组到底几组各组多少例、连续变量分布）喂回代码生成——从根上消灭
  “臆测列名/结构”这一数据分析头号失败源。探索失败自动跳过，不影响后续。
- **确定性方法路由 + 决策卡（T3）**：新增 `statroute.py`。AI 只做“把研究问题拆成分析规格”
  的语义抽取，**由代码在真实数据上跑前提检验（正态性 Shapiro / 方差齐性 Levene / 期望频数）
  并用决策树选统计方法**（t 检验 / Welch / Mann-Whitney / ANOVA / Kruskal / 卡方 / Fisher /
  相关…），再把选定方法喂回代码生成。这样 AI 没机会“前提没满足却硬跑参数检验”。选定方案以
  “决策卡”展示（数据情况 / 前提检查 / 建议方法 / 备选），也是你核对“分组有没有搞反”的关口。
- **更聪明的自动修复（T4）**：修复时不再只喂报错——附上真实数据诊断（`df.shape`、各列 dtype），
  并要求模型“先说错因再改、可改用 scipy/statsmodels 绕开库问题”；若在**同一个错误上反复失败**，
  自动切换为“换一种思路/库从头重写”（而非继续贴补丁）。
- **产品红线（T4）**：结论区固定提示“AI 生成、正式使用前请由专业统计人员核对；显著≠临床意义”，
  结论措辞也要求审慎（统计显著不等于因果/临床意义）。

### 数据分析：提升任务成功率（T1 确定性快赢）

- **统一数据读取**：子进程执行不再自行重读原始文件，改由主进程用统一的健壮解码
  （`textio.read_csv_bytes`）读入并去重列名后、以 pickle 传给子进程。彻底消除“AI 看到的
  列名”与“执行时真实列名”因两套解码不一致而导致的 KeyError（本该成功却失败）。
- **更聪明的数据画像**：画像新增数值列描述统计、分类列主要取值(计数)，并对每列做启发式
  体检——自动标注“看着是文本其实是数字”（含千分位/百分号/货币/删失阈值 `<`）、疑似日期列、
  疑似 ID/编号列、未被识别的缺失标记（`-`/`缺失`/`999` 等），提示 AI 先清洗再分析。
- **结果确定性体检**：分析跑通后用规则（不调用 AI）核对输出——`p=0.000` 应写为 `p<0.001`、
  p 值越界、缺少必需的【方法选择】/【假设检查】/【数据质量】区块——并把问题喂回结论环节让
  AI 据实修正或说明。
- **修复：危险调用护栏误杀**——扫描前先剥离字符串与注释，图表标题/print 文本里出现的
  `open(`、`eval(` 等不再被误判而整段拒绝执行（真实的系统/网络调用仍拦截）。
- **修复：结论输入截断丢数字**——结论所依据的真实输出过长时改为保留头尾（尾部常含主分析
  结果/p 值），不再一刀切截断前 6000 字。
- **修复：统计顾问结果卡崩溃**——前端按后端契约（`recommended={test,why}`、
  `alternatives=[{test,when}]`）渲染对象，不再因把对象当 React 子节点而报错。
