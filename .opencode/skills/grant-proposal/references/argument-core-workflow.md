> 本文件从 SKILL.md「工作流程总则 + 第 3.5 步 · 论证内核」搬出，由 SKILL.md 在进入第 3.5 步（论证内核）时引用。下面是该节的完整原文（SKILL.md 正文只保留要点与指针）。

## 工作流程总则中关于第 3.5 步的说明

> ⚠️ **别把这条流程读成"合规检查表"**：第 1–3 步解决"写给谁、写多长、什么格式"，**第 3.5 步才解决"写什么"**。
> 格式全对、字数全合、形审全过，但立项依据是文献罗列、创新点没有对照物——这样的稿子照样出局，而且是最常见的出局方式。
> **第 3.5 步与第 5 步的空洞度闸不许跳过**，它们和字数硬限一样是硬要求。

### 第 3.5 步 · 论证内核（**动笔前必做，别跳**）

前三步解决的是"写给谁、写多长、什么格式"；**这一步解决"写什么"**。跳过它，立项依据必然退化成
"A 报道了…B 发现了…然而机制尚不清楚，亟待进一步阐明"——每句都没错，评审读完却不知道
**哪一步卡住、为什么至今没解决、你凭什么能解决**。这是标书最常见的死法，也是本技能一度最弱的一环。

**⓪ 先看有没有现成的设计定案**：扫当前工作目录有无 `design_brief.md`（idea-forge 立意锻打对话的出件，标书变体含立意卡/创新点五要素/方案骨架/证据台账，通常还伴随 `closest_work.md`）——**有就直接映射成本步四件套，跳过下面 1–4 的自行推导**（用户与 idea-forge 逐节确认过的结论，别再翻案；只补映射时发现的明显缺口）。**没有**且用户在线 → 照 §六 给编号选项："**1)** 先进 `idea-forge` 用多轮对话把立意/创新点/方案锻硬再写（推荐，标书空洞的根因就是这步没做过；它现在开场先让你一轮批量自答，不再逐条盘问）　**2)** 我直接说立意与创新点，你据此做**最小核实**再起草（快车道：想清楚了、只差落纸时选这条）　**3)** 快速通道：我按下面 1–4 自行检索推导（最省事，但吃不到你的临床观察与未写出的前期思考）"。用户回 3、或无人值守场景，才走下面四个动作。

**用户回 2（快车道）怎么走**：让他用一段话说出「痛点 + 关键科学问题 + 2–3 条创新点 + 打算怎么做」，我据此直接填四件套，**只做最小核实**——① 他宣称的"首次/没人做过"必须查（会写进正文的可证伪断言，不核就是把错误自信印进标书）；② 他点名的最接近工作只核实那几篇、不做泛检索；③ 其余采信，缺的按 ⏸ 记 Minor。核实中查到反例 → 摆证据给出路（照 §六 编号），别自作主张改他的创新点。

**先 Read `references/rationale-and-innovation.md`**，然后按它跑完四个动作，**产出物齐了才进第 4 步**：

1. **读上游产物**（该卡 §1）：扫当前工作目录有无 `research_scan.md`（research-scan）、`topic_selection*.md`、`evidence_table.csv`（多轮检索时另有 `evidence_table__<标签>.csv`）/ 综述稿——有就直接映射，别重做泛综述。**一个都没有**（用户直呼本技能、跳过了上游）→ **先问用户一句**："你知道**谁的工作最接近**这个课题吗？**1)** 我知道，是……（点名即可，我去核实）　**2)** 不知道，你去查"——用户点得出名字（多数情况点得出）→ **只核实他点的那几篇**建对照表，成本低一个量级且更准；他空手 → 才用 `literature-review` 的 `search.py` 或 `search-lit` 做**针对性检索**，目标只有一个：找出与本课题**最接近的 3–5 项工作**并读到能说清其失效条件（必要时 `fulltext-retrieval` 取全文）。检索式围绕**你的科学问题**构造，不是围绕疾病名——查疾病名只会捞回一万篇背景。
2. **建「最接近工作对照表」**（该卡 §2）→ 写 `closest_work.md`，≤5 行：`最接近工作(引文) | 它回答了什么 | 具体局限 | 本课题的增量 | 该增量为何以前做不到`。局限要按**八类失配**（人群/终点/时间尺度/机制层次/测量/设计/规模/一致性）具体到设计参数，**不接受"研究较少""不够深入"**。**没有这张表不许写创新点。**
3. **gap 归因四选一**（该卡 §3）：这个空白**为什么至今还在**——技术不可及 / 数据不可得 / 概念未提出 / 做过但证据互斥。这一问就是用户说的"深入分析"之所在，且**归因直接决定创新点类型与可行性怎么论**。答不出来 = 空白要么是假的、要么不值得做。归因①而拿不出技术可及性证据（前期数据/平台）→ 按 §六 给编号选项停下问用户，别硬写。
4. **锁一句可证伪的关键科学问题**（该卡 §4）：从上面的局限 + 归因**收敛**出来，单句、可证伪、能追溯到对照表的某一行。放哪按渠道分（2026 新格式写进「研究内容」开头 + 摘要；classic 类别单列进「拟解决的关键科学问题」栏；NIH 放 Specific Aims 首段；指标驱动渠道换成"关键技术瓶颈 + 考核指标"）。

**缺用户输入时**（该卡 §8）：深入分析吃两样用户独有的东西——真实临床观察与前期数据。**能枚举的用编号选项问**（如"你觉得这个空白至今还在，主因是哪一类？1) 缺测量手段 2) 缺样本数据 3) 没人这么设想过 4) 结论互相打架"），比"请介绍研究背景"有用得多；纯事实项开放式要，缺则标 `[待补充]`——照 AGENTS.md §五 判级铁律，**这属 Minor，不阻断交付、不触发返工**。**绝不用编造填补**（编前期数据、编"最接近工作"、编检索结论都踩不虚构红线）。
