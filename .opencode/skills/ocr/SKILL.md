---
name: ocr
description: 把图片/扫描件里的文字识别出来（中文为主，含英文数字）。用于官方以图片形式发布的文件（如国自然申请代码表、学部注意事项、政策公文截图）、拍照/扫描的表格与证件、图片版 PDF 的书页等——凡是"文字在像素里、复制不出来"的情形。调 OCR.space 托管 API，**模型在云端跑、本机与服务器都不装 OCR 模型**（不加负担）。当用户说"这张图/截图里的字提取一下""扫描件转文字""这个 PDF 是图片版复制不了""官方文件是图片""OCR""识别一下这张表"时使用。要下载 PDF/把文字版 PDF 转 md 用 fulltext-retrieval；要给患者数据脱敏用 deidentify。
---

# OCR 图片识字技能

> **产物位置**：写到当前工作目录（会话产物目录），裸文件名，别拼 `outputs/` 前缀、别写仓库根。

把"文字印在图片里、选不中也复制不了"的内容识别成可用文本。典型场景：国自然把申请代码表、学部注意事项等**以 JPG 发布**，普通网页抓取（WebFetch）只能拿到目录框架、拿不到图里的字；用户拍照/扫描的表格、公文、证件；图片版 PDF 的书页。

## 铁律：signal not verdict（先读这条）
OCR 是**辅助提取**，不是权威原件。Engine3 中文准确度很高，但**个别生僻字、相近字形（O/0、己/已、末/未）、密排数字仍可能出错**。因此：
- **凡是"错一个字就出事"的内容**——申请代码、金额、批号、注册号、剂量、身份证/银行卡号、日期——**必须人工/交叉核对**后再用，别把 OCR 结果当定稿。
- 交付时**标注这是 OCR 结果、建议对关键字段复核**；能对照原图的就对照，能找到官方文字版的以官方为准。
- 完整性可自查：连续编号（代码表、条目表）OCR 后检查有没有跳号，跳号=漏行。

## 依赖与 key
- **API key**：环境变量 `OCR_SPACE_API_KEY`。多用户容器**已由部署注入**（`deploy/.env` → `render-compose.sh`），会话里直接可用。本机手动跑时先 `export OCR_SPACE_API_KEY=<key>`（免费注册 https://ocr.space/ocrapi ，邮箱即可，**别写进仓库**）。
- **Python**：项目根 `.venv`（脚本自动解析；缺则先跑 `env-setup`）。`requests` 必需，`Pillow` 用于压缩 >1MB 的图。
- **免费额度**（所有用户容器共用同一个 key 的配额，注意别刷爆）：Engine3 **2500 次/月**、Engine1/2 25000/月、500 次/天/IP、**单图 ≤1MB**（脚本自动压缩超标图）。

## 用法
```bash
# 单张（URL 或本地路径都行）
bash ${REPO_ROOT:-/app}/.opencode/skills/ocr/scripts/ocr.sh 图片URL或路径

# 多张按顺序识别，输出间以 "===== FILE: <名> =====" 分隔
bash ${REPO_ROOT:-/app}/.opencode/skills/ocr/scripts/ocr.sh a.jpg b.png c.jpg > out.txt

# 官方页面里的图先解析出真实图片 URL 再喂进来（页面常是 <img src=...jpg>）
```
脚本做的事：取图（URL 下载 / 本地读）→ >1MB 自动用 Pillow 压缩到 1MB 内 → POST 到 OCR.space（`language=chs, OCREngine=3, isTable=true`）→ 输出识别文本。

## 典型流程（官方图片文件 → 可用文本）
1. 目标是网页里的图 → 先用 `.venv` 的 requests+bs4 解析页面 `<img>` 的 src，拿到真实图片 URL（NSFC 等站点直接抓页面可能 412，加 User-Agent）。
2. 逐图跑 `ocr.sh`。
3. **核对**：对照原图抽查关键字段；连续编号查跳号；关键数字/代码交叉核实。
4. 落地：写进产物或（维护场景）更新对应参考资料，并注明"OCR 自官方图片、口径年份、关键字段建议系统内复核"。

## 局限（如实告知用户）
- 单图 ≤1MB（脚本自动压缩，极密的大图压缩后可能掉精度 → 可让用户提供更清晰的原图或分块）。
- 手写体、艺术字、极低分辨率、盖章/水印遮挡处准确度下降。
- 不做版面语义理解（不还原合并单元格的复杂表格逻辑）——只出文字与大致行序。
- 免费额度共享，大批量扫描前先估算张数；不够时降 Engine1/2 或换其他后端。

## 衔接
- 官方以图片发布的标书/政策文件 → 本技能提取后供 `grant-proposal` 更新要求卡（那边"动态核实闸"遇到图片文件即走本技能）。
- 图片版 / 扫描版 PDF → 先用 `fulltext-retrieval` 取得 PDF，再逐页图 OCR（或直接喂图片版页面）。
- 含患者信息的图 → 提取后若要入库分析，先走 `deidentify` 脱敏。

## 约定
- 不虚构：识别不出的地方标"[识别不清]"，别脑补填字。
- 关键字段（代码/金额/批号/剂量）一律标注"待人工复核"，不下"就是这个"的定论。
