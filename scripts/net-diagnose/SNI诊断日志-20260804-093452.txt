════════ SNI 归因诊断  2026-08-04 09:34:52 ════════
目标：47.86.27.60:443   域名：weigu.duckdns.org
⚠ 系统代理已开启（127.0.0.1:7897）——本脚本用裸 socket、不走代理，结论仍有效，但浏览器的表现可能不同

[A] 连 47.86.27.60:443，只把 ClientHello 里的域名(SNI)换掉：
    本域名  weigu.duckdns.org                         → 握手成功  235ms
    无关域名 www.microsoft.com                         → 服务器正常应答(TLS警报/证书不符)  125ms
        The Local Security Authority cannot be contacted
    变形域名 aweigu.duckdns.org（只多一个字母）                → 服务器正常应答(TLS警报/证书不符)  119ms
        The Local Security Authority cannot be contacted
    不带域名（SNI 用 IP，等于不发 SNI）                        → 服务器正常应答(TLS警报/证书不符)  1070ms
        The Local Security Authority cannot be contacted

[B] 本域名重复 3 次（看是不是每次必掐）：
    第 1 次 → 握手成功  83ms
    第 2 次 → 握手成功  88ms
    第 3 次 → 握手成功  103ms

[C] 对照组（证明本网络的 HTTPS 本身是好的）：
    www.baidu.com:443                              → 握手成功  111ms

[D] 明文 HTTP 80（Host 头带域名，看黑名单是不是按域名匹配）：
    Host: weigu.duckdns.org                        → 被强行掐断(RST)
    Host: www.microsoft.com                        → HTTP 308

──────── 结论 ────────
这次没能复现（本域名握手没有被掐）。故障可能是间歇性的，或者换了出口/时段。
请在【故障复现的那一刻】重跑本脚本。
另外：80 端口带本域名 Host 也被掐 —— 黑名单在明文 HTTP 层同样按域名匹配。

