════════ SNI 归因诊断  2026-08-04 10:09:10 ════════
目标：47.86.27.60:443   域名：niuma.tellgen.com
⚠ 系统代理已开启（127.0.0.1:7897）——本脚本用裸 socket、不走代理，结论仍有效，但浏览器的表现可能不同

[A] 连 47.86.27.60:443，只把 ClientHello 里的域名(SNI)换掉：
    本域名  niuma.tellgen.com                         → 服务器正常应答(TLS警报/证书不符)  90ms
        The Local Security Authority cannot be contacted
    无关域名 www.microsoft.com                         → 服务器正常应答(TLS警报/证书不符)  104ms
        The Local Security Authority cannot be contacted
    变形域名 aniuma.tellgen.com（只多一个字母）                → 服务器正常应答(TLS警报/证书不符)  96ms
        The Local Security Authority cannot be contacted
    不带域名（SNI 用 IP，等于不发 SNI）                        → 服务器正常应答(TLS警报/证书不符)  94ms
        The Local Security Authority cannot be contacted

[B] 本域名重复 3 次（看是不是每次必掐）：
    第 1 次 → 服务器正常应答(TLS警报/证书不符)  76ms
    第 2 次 → 服务器正常应答(TLS警报/证书不符)  72ms
    第 3 次 → 服务器正常应答(TLS警报/证书不符)  85ms

[C] 对照组（证明本网络的 HTTPS 本身是好的）：
    www.baidu.com:443                              → 握手成功  36ms

[D] 明文 HTTP 80（Host 头带域名，看黑名单是不是按域名匹配、匹配到哪一级）：
    Host: niuma.tellgen.com                        → HTTP 308
    Host: zzztest.tellgen.com  (同主域的另一个子域)         → HTTP 308
    Host: www.microsoft.com  (无关域名，对照)             → HTTP 308

[E] 同样的 Host 打到一台无关服务器（排除是我们服务器自己的问题）：
    对照服务器：180.101.49.44（www.baidu.com）
    Host: niuma.tellgen.com                        → HTTP 403
    Host: www.baidu.com                            → HTTP 200

──────── 结论 ────────
本次全通，没有发现按域名或按 IP 的拦截。
若用户仍报「打不开」，那多半不在网络层（看浏览器代理设置、客户端里写死的旧地址、或应用侧报错）。

