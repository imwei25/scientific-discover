科研助手 Windows 桌面安装包。

- 下载 `*-setup.exe` 后双击安装，终端用户无需安装 Python 或任何依赖。
- 首次打开会在 `%APPDATA%\科研助手\.env` 自动生成配置模板；
  用记事本填入你的 `LLM_API_KEY`（默认 DeepSeek，也可换硅基流动/OpenAI/Claude），
  保存后重新打开应用即可使用 AI 功能。
- 数据分析、画图等在本机完成，数据不出网。
